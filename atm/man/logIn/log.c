#include "log.h"

void log_in(){
    /*char email[30], pass[30], fileName[100]={"../gen/accounts/"};
    char buffer[MAX_LINE_SIZE];
    
    again:
    printf("\nLog in page:\n\n");
    printf("\nEmail: ");
    scanf("\n");
    scanf("%[^\n]",email);
    printf("Password: ");
    scanf("\n");
    scanf("%[^\n]",pass);
    printf("\nLoading ...\n");
    
    strcat(fileName,email);
    strcat(fileName,".txt");

    FILE* log = fopen(fileName,"r");
    // The file don't open means there is no file with the email enterd by the user
    if(log == NULL){
        system("clear");
        printf("\nemail or password incorrect please try again\n");
        goto again;
    }

    while(fgets(buffer, sizeof(buffer), log)){
        // scan the password from file
    }
    if(strcmp(pass,buffer) == 0) printf("Succes");
    else goto again;

    fclose(log);*/

}